# =============================================================================
# Restorae Admin Dashboard - PRODUCTION Environment Variables
# =============================================================================
# Copy this file to .env.production and fill in your production values
# NEVER commit .env.production with real values to version control
# =============================================================================

# -----------------------------------------------------------------------------
# Backend API Configuration
# -----------------------------------------------------------------------------
# Public URL (used by browser)
NEXT_PUBLIC_API_URL=https://api.restorae.app
NEXT_PUBLIC_API_PREFIX=/api/v1

# Internal URL (used by server-side, e.g., Docker network)
# If running in Docker, use the container name
INTERNAL_API_URL=http://backend:3001

# -----------------------------------------------------------------------------
# NextAuth Configuration
# -----------------------------------------------------------------------------
# The public URL where the admin dashboard is hosted
NEXTAUTH_URL=https://admin.restorae.app

# Generate a secure secret: openssl rand -base64 32
NEXTAUTH_SECRET=your-nextauth-secret-min-32-characters-long

# -----------------------------------------------------------------------------
# Session Configuration
# -----------------------------------------------------------------------------
# Session expires in 24 hours by default (in seconds)
SESSION_MAX_AGE=86400
